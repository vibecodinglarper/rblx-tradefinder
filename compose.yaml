services:
  bot:
    build: .
    restart: unless-stopped
    env_file: .env
    environment:
      DATABASE_PATH: /app/data/tradefinder.sqlite
    volumes:
      - bot-data:/app/data
volumes:
  bot-data:
